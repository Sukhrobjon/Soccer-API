{{!-- NAVIGATION --}}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Soccer API</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#">About <span class="sr-only">(current)</span></a>
            </li>

            <li class="nav-item active">
                <a class="nav-link" href="https://sukhrobjon.github.io/Soccer-API/#/">Documention <span class="sr-only">(current)</span></a>
            </li>
        
        </ul>
        <form class="form-inline my-2 my-lg-0">
            {{# if currentUser }}
                <input class="btn btn-outline-primary my-2 my-sm-0 mr-sm-2" type="button" value="Logout"
                    onclick="window.location.href='/logout'" />
                {{!-- <div class="pull-right"> --}}
                <a href="/leagues/new" class="btn btn-primary navbar-btn">Add League</a>
                {{!-- </div> --}}
            {{else}}
                <input class="btn btn-outline-primary my-2 my-sm-0 mr-sm-2" type="button" value="Login"
                    onclick="window.location.href='/login'" />
                <input class="btn btn-outline-primary my-2 my-sm-0" type="button" value="Register"
                    onclick="window.location.href='/sign-up'" />
            {{/if}}
            
        </form>
    </div>
</nav>

